const {describe, it} = require('mocha');
const assert = require('assert');
const fetchPamResult = require('./fetchPamResult');
describe('fetch-pam-result', function() {
  it('fetch-pam-result', async function() {
    //const pamResult = await fetchPamResult("direktør");
    //console.log(pamResult);
  });
});
